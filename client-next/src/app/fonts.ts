import { Coiny, Fustat } from "next/font/google";

export const coinySans = Coiny({
    variable: '--font-coiny-sans',
    weight: '400',
    subsets: ["latin"],
  });
  
 export const fustatSans = Fustat({
    variable: "--font-fustat-sans",
    style: ['normal'],
    subsets: ["latin"],
  });
  